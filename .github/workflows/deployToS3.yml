name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: myawsbucket-hosting
        AWS_ACCESS_KEY_ID: AKIAWF5PWDYOMPBGXGHO
        AWS_SECRET_ACCESS_KEY: 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 
        AWS_REGION: 'ap-south-1'   # optional: defaults to us-east-1
       
