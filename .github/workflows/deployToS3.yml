name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: myawsbucket-hosting
        AWS_ACCESS_KEY_ID: AKIAWF5PWDYOBGB6JKM4
        AWS_SECRET_ACCESS_KEY: 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
        AWS_REGION: 'ap-south-1'   # optional: defaults to us-east-1
       
